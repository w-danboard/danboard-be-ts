import chai from 'chai'
import chaiHTTP from 'chai-http'
import { sequelize, User } from '../model'

chai.use(chaiHTTP)

// 会在所有的单元测试之前执行
before(async () => {

})

// 会在每个单元测试之前执行
beforeEach(async () => {

})

// 会在每个单元测试之后执行
afterEach(async () => {

})

// 会在所有的单元测试之后执行
after(async () => {

})